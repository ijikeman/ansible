---
- name: Service start {{ daemon_name }}
  service:
    name: "{{ daemon_name }}"
    state: started
  listen: Service start {{ daemon_name }}

- name: Service restart {{ daemon_name }}
  service:
    name: "{{ daemon_name }}"
    state: restarted
  listen: Service restart {{ daemon_name }}

- name: Service reloaded {{ daemon_name }}
  service:
    name: "{{ daemon_name }}"
    state: reloaded
  listen: Service reloaded {{ daemon_name }}

- name: Service stop {{ daemon_name }}
  service:
    name: "{{ daemon_name }}"
    state: stopped
  listen: Service stop {{ daemon_name }}

- name: Service enabled {{ daemon_name }}
  service:
    name: "{{ daemon_name }}"
    enabled: yes
  listen: Service enabled {{ daemon_name }}

- name: Service disabled {{ daemon_name }}
  service:
    name: "{{ daemon_name }}"
    enabled: no
  listen: Service disabled {{ daemon_name }}

- name: Systemd start {{ daemon_name }}
  systemd:
    name: "{{ daemon_name }}"
    state: started
  listen: Systemd start {{ daemon_name }}

- name: Systemd restart {{ daemon_name }}
  systemd:
    name: "{{ daemon_name }}"
    state: restarted
  listen: Systemd restart {{ daemon_name }}

- name: Systemd reloaded {{ daemon_name }}
  systemd:
    name: "{{ daemon_name }}"
    state: reloaded
  listen: Systemd reloaded {{ daemon_name }}

- name: Systemd stop {{ daemon_name }}
  systemd:
    name: "{{ daemon_name }}"
    state: stopped
  listen: Systemd stop {{ daemon_name }}

- name: Systemd enabled {{ daemon_name }}
  systemd:
    name: "{{ daemon_name }}"
    enabled: yes
  listen: Systemd enabled {{ daemon_name }}

- name: Systemd disabled {{ daemon_name }}
  systemd:
    name: "{{ daemon_name }}"
    enabled: no
  listen: Systemd disabled {{ daemon_name }}
