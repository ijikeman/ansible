---
- name: Service start
  service:
    name: "{{ daemon_name }}"
    state: started
    enabled: yes

- name: Service restart
  service:
    name: "{{ daemon_name }}"
    state: restarted

- name: Service stop
  service:
    name: "{{ daemon_name }}"
    state: stopped
    enabled: no
