---
- name: Install
  get_url:
    url: "{{ URL_DOCKER_COMPOSE }}/{{ VERSION_DOCKER_COMPOSE }}/docker-compose-Linux-x86_64"
    dest: /usr/local/bin/docker-compose
    owner: 'root'
    group: 'root'
    mode: '0755'
    sha256sum: "{{ SHA256SUM_DOCKER_COMPOSE }}"
    force: yes
