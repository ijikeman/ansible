---
- name: Set backup script
  template:
    src: backup.sh.j2
    dest: "{{ IMG_DIR }}/bin/backup.sh"
    owner: 0
    group: 0
    mode: 0700

- name: Set backup script to crond
  cron:
    name: "backup script"
    minute: "0"
    hour: "0"
    job: "{{ IMG_DIR }}/bin/backup.sh"
