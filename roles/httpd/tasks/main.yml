- name: Install
  dnf:
    name: "httpd"
    state: "present"

- name: Config
  template:
    src: "test"
    dest: "/tmp/test_httpd"
  notify: "{{ NAME_SERVICE_MANAGER }} {{ NAME_DAEMON }} restarted"
