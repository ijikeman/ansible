---
- name: Docker install
  include_tasks: install_{{ ansible_os_family }}_{{ ansible_distribution_major_version }}.yml

- name: Docker enable
  include_tasks: state_{{ ansible_os_family }}_{{ ansible_distribution_major_version }}.yml

- name: Docker setup
  include_tasks: setup_{{ ansible_os_family }}_{{ ansible_distribution_major_version }}.yml

- name: Docker start
  include_tasks: start_{{ ansible_os_family }}_{{ ansible_distribution_major_version }}.yml
