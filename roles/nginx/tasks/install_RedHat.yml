---
- name: add repository
  template:
    src: nginx.repo.j2
    dest: /etc/yum.repos.d/nginx.repo
    owner: root
    group: root
    mode: 0644
  # when: nginx_yum_repo_enabled

- name: install
  yum:
    name: nginx
    enablerepo: nginx
